// galleryModel.js
const db = require('../config/dbConfig.js');

exports.fetchAll = () => {
	// Logic to interact with DB and get all gallery entries
};

exports.insert = (postData) => {
	// Logic to insert a new gallery entry into the DB
};

exports.getDetail = (postId) => {
	// Logic to fetch detailed gallery entry from DB
};
