// plansModel.js
const db = require('../config/dbConfig.js');

exports.fetchAll = () => {
	// Logic to interact with DB and get all plans entries
};

exports.insert = (postData) => {
	// Logic to insert a new plans entry into the DB
};

exports.getDetail = (postId) => {
	// Logic to fetch detailed plans entry from DB
};
