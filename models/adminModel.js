// adminModel.js
const db = require('../config/dbConfig.js');

exports.fetchAll = () => {
	// Logic to interact with DB and get all admin entries
};

exports.insert = (postData) => {
	// Logic to insert a new admin entry into the DB
};

exports.getDetail = (postId) => {
	// Logic to fetch detailed admin entry from DB
};
