// coursesModel.js
const db = require('../config/dbConfig.js');

exports.fetchAll = () => {
	// Logic to interact with DB and get all courses entries
};

exports.insert = (postData) => {
	// Logic to insert a new courses entry into the DB
};

exports.getDetail = (postId) => {
	// Logic to fetch detailed courses entry from DB
};
