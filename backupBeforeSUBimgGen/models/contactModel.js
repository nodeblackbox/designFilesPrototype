// contactModel.js
const db = require('../config/dbConfig.js');

exports.fetchAll = () => {
	// Logic to interact with DB and get all contact entries
};

exports.insert = (postData) => {
	// Logic to insert a new contact entry into the DB
};

exports.getDetail = (postId) => {
	// Logic to fetch detailed contact entry from DB
};
